version: '3.8'

services:
  active-mq:
    build: ./active-mq/
    ports:
      - "8161:8161"
      - "61616:61616"

  event-service:
    build:
      dockerfile: ./event-service/Dockerfile
      context: ./
    ports:
      - "8080"

  payment-service:
    build:
      dockerfile: ./payment-service/Dockerfile
      context: ./
    ports:
      - "8080"
    depends_on:
      - active-mq
